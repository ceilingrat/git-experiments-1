[tool.poetry]
name = "git-experiments-1"
version = "0.1.0"
description = ""
authors = ["ceilingrat <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
asciinema = "^2.0.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
